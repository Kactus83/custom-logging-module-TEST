import { LogLevel } from "../types/LogLevel";
import ServiceMetadata from "../types/ServiceMetadata";
import { ColorChoice } from "../types/TerminalColors";
import { LoggerMode } from "../types/LoggerMode";
export declare abstract class LoggerClient {
    protected serviceMetadata: ServiceMetadata;
    constructor(loggerMode: LoggerMode, serviceName: string, isMainProcess?: boolean, logLevel?: LogLevel, color?: ColorChoice);
    protected log(level: LogLevel, message: string): void;
    private ensureLoggingServiceInitialized;
}
