import { LoggerConfig } from "../models/LoggerConfig";
import { LoggerStylesConfig } from "../models/LoggerStylesConfig";
import ServiceMetadata from "../types/ServiceMetadata";
import { LogLevel } from "../types/LogLevel";
export declare class LoggingService {
    private static instance;
    private loggerConfig?;
    private loggerStyleService?;
    private servicesMetadata;
    private constructor();
    static getInstance(): LoggingService;
    init(loggerConfig: LoggerConfig): void;
    static initialize(loggerConfig: LoggerConfig, loggerStylesConfig?: LoggerStylesConfig): void;
    static isInitialized(): boolean;
    updateConfig(loggerConfig: LoggerConfig): void;
    log(metadata: ServiceMetadata, level: LogLevel, message: string): void;
    private shouldLog;
    registerServiceIfNeeded(metadata: ServiceMetadata): void;
    private findServiceMetadata;
    private processConsoleLog;
}
