import { LogLevel } from "../../../types/LogLevel";
import ServiceMetadata from "../../../types/ServiceMetadata";
export declare class LoggerStyleService {
    constructor();
    formatMessage(processMetadata: ServiceMetadata, level: LogLevel, message: string): string;
    private getColorCode;
    private applyStyle;
}
