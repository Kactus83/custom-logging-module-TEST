import { LoggerMode } from "../types/LoggerMode";
import { LogLevel } from "../types/LogLevel";
export declare class LoggerConfig {
    logLevel: LogLevel;
    loggerMode: LoggerMode;
    constructor(logLevel?: LogLevel, loggerMode?: LoggerMode);
}
